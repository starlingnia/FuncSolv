# this is the build file for project 
# it is autogenerated by the xmake build system.
# do not edit by hand.

# project
cmake_minimum_required(VERSION 3.15.0)
cmake_policy(SET CMP0091 NEW)
project(FuncSolv_tasks LANGUAGES CXX)
set(CURRENT_COMPILER_ID ${CMAKE_CXX_COMPILER_ID})
set(CURRENT_COMPILER_FRONTEND_VARIANT ${CMAKE_CXX_COMPILER_FRONTEND_VARIANT})
if(NOT CURRENT_COMPILER_FRONTEND_VARIANT)
    if(MSVC)
        set(CURRENT_COMPILER_FRONTEND_VARIANT "MSVC")
    else()
        set(CURRENT_COMPILER_FRONTEND_VARIANT "GNU")
    endif()
endif()
if(DEFINED CMAKE_CXX_COMPILER_LINKER_FRONTEND_VARIANT)
    set(CURRENT_LINKER_FRONTEND_VARIANT ${CMAKE_CXX_COMPILER_LINKER_FRONTEND_VARIANT})
endif()
if(NOT CURRENT_LINKER_FRONTEND_VARIANT)
    set(CURRENT_LINKER_FRONTEND_VARIANT ${CURRENT_COMPILER_FRONTEND_VARIANT})
endif()

# target
add_library(formergesortlists SHARED "")
set_target_properties(formergesortlists PROPERTIES OUTPUT_NAME "formergesortlists")
set_target_properties(formergesortlists PROPERTIES LIBRARY_OUTPUT_DIRECTORY "${CMAKE_SOURCE_DIR}/lib")
add_dependencies(formergesortlists FuncSolv_core)
target_include_directories(formergesortlists PRIVATE
    include
)
target_compile_options(formergesortlists PRIVATE
    $<$<COMPILE_LANGUAGE:C>:-target>
    $<$<COMPILE_LANGUAGE:CXX>:-target>
    $<$<COMPILE_LANGUAGE:C>:arm64-apple-macos>
    $<$<COMPILE_LANGUAGE:CXX>:arm64-apple-macos>
    $<$<COMPILE_LANGUAGE:C>:-isysroot>
    $<$<COMPILE_LANGUAGE:CXX>:-isysroot>
    $<$<COMPILE_LANGUAGE:C>:/Library/Developer/CommandLineTools/SDKs/MacOSX.sdk>
    $<$<COMPILE_LANGUAGE:CXX>:/Library/Developer/CommandLineTools/SDKs/MacOSX.sdk>
    $<$<COMPILE_LANGUAGE:C>:-DNDEBUG>
    $<$<COMPILE_LANGUAGE:CXX>:-DNDEBUG>
    $<$<COMPILE_LANGUAGE:CUDA>:-DNDEBUG>
)
set_target_properties(formergesortlists PROPERTIES CXX_EXTENSIONS OFF)
target_compile_features(formergesortlists PRIVATE cxx_std_26)
if(CURRENT_COMPILER_FRONTEND_VARIANT STREQUAL "MSVC")
    target_compile_options(formergesortlists PRIVATE $<$<CONFIG:Release>:-O2>)
else()
    target_compile_options(formergesortlists PRIVATE -O3)
endif()
if(CURRENT_COMPILER_FRONTEND_VARIANT STREQUAL "MSVC")
    set_property(TARGET formergesortlists PROPERTY
        MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>")
endif()
target_link_libraries(formergesortlists PRIVATE
    FuncSolv_core
)
target_link_directories(formergesortlists PRIVATE
    lib
)
target_link_options(formergesortlists PRIVATE
    -target
    arm64-apple-macos
    -isysroot
    /Library/Developer/CommandLineTools/SDKs/MacOSX.sdk
    -lz
)
# private sourcefiles from sourcebatch c++.build for target formergesortlists
target_sources(formergesortlists PRIVATE 
    tools/Services/MSL.cpp
    tools/Services/WaterVolumeService.cpp
)

# target
add_executable(FuncSolv "")
set_target_properties(FuncSolv PROPERTIES OUTPUT_NAME "FuncSolv")
set_target_properties(FuncSolv PROPERTIES RUNTIME_OUTPUT_DIRECTORY "${CMAKE_SOURCE_DIR}/bin")
add_dependencies(FuncSolv FuncSolv_core)
target_include_directories(FuncSolv PRIVATE
    include
)
target_compile_options(FuncSolv PRIVATE
    $<$<COMPILE_LANGUAGE:C>:-target>
    $<$<COMPILE_LANGUAGE:CXX>:-target>
    $<$<COMPILE_LANGUAGE:C>:arm64-apple-macos>
    $<$<COMPILE_LANGUAGE:CXX>:arm64-apple-macos>
    $<$<COMPILE_LANGUAGE:C>:-isysroot>
    $<$<COMPILE_LANGUAGE:CXX>:-isysroot>
    $<$<COMPILE_LANGUAGE:C>:/Library/Developer/CommandLineTools/SDKs/MacOSX.sdk>
    $<$<COMPILE_LANGUAGE:CXX>:/Library/Developer/CommandLineTools/SDKs/MacOSX.sdk>
    $<$<COMPILE_LANGUAGE:C>:-DNDEBUG>
    $<$<COMPILE_LANGUAGE:CXX>:-DNDEBUG>
    $<$<COMPILE_LANGUAGE:CUDA>:-DNDEBUG>
)
set_target_properties(FuncSolv PROPERTIES CXX_EXTENSIONS OFF)
target_compile_features(FuncSolv PRIVATE cxx_std_26)
if(CURRENT_COMPILER_FRONTEND_VARIANT STREQUAL "MSVC")
    target_compile_options(FuncSolv PRIVATE $<$<CONFIG:Release>:-O2>)
else()
    target_compile_options(FuncSolv PRIVATE -O3)
endif()
if(CURRENT_COMPILER_FRONTEND_VARIANT STREQUAL "MSVC")
else()
    target_compile_options(FuncSolv PRIVATE -fvisibility=hidden)
endif()
if(CURRENT_COMPILER_FRONTEND_VARIANT STREQUAL "MSVC")
    set_property(TARGET FuncSolv PROPERTY
        MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>")
endif()
target_link_libraries(FuncSolv PRIVATE
    FuncSolv_core
)
add_library(target_objectfiles_FuncSolv_7ee26cdcc007ddc6 INTERFACE)
target_link_libraries(target_objectfiles_FuncSolv_7ee26cdcc007ddc6 INTERFACE
    FuncSolv_tasks
)
target_link_libraries(FuncSolv PRIVATE target_objectfiles_FuncSolv_7ee26cdcc007ddc6)
target_link_directories(FuncSolv PRIVATE
    lib
)
target_link_options(FuncSolv PRIVATE
    -target
    arm64-apple-macos
    -isysroot
    /Library/Developer/CommandLineTools/SDKs/MacOSX.sdk
    -lz
)
# private sourcefiles from sourcebatch c++.build for target FuncSolv
target_sources(FuncSolv PRIVATE 
    apps/main.cpp
)

# target
add_library(FuncSolv_core STATIC "")
set_target_properties(FuncSolv_core PROPERTIES OUTPUT_NAME "FuncSolv_core")
set_target_properties(FuncSolv_core PROPERTIES ARCHIVE_OUTPUT_DIRECTORY "${CMAKE_SOURCE_DIR}/lib")
target_include_directories(FuncSolv_core PRIVATE
    include
)
target_compile_options(FuncSolv_core PRIVATE
    $<$<COMPILE_LANGUAGE:C>:-target>
    $<$<COMPILE_LANGUAGE:CXX>:-target>
    $<$<COMPILE_LANGUAGE:C>:arm64-apple-macos>
    $<$<COMPILE_LANGUAGE:CXX>:arm64-apple-macos>
    $<$<COMPILE_LANGUAGE:C>:-isysroot>
    $<$<COMPILE_LANGUAGE:CXX>:-isysroot>
    $<$<COMPILE_LANGUAGE:C>:/Library/Developer/CommandLineTools/SDKs/MacOSX.sdk>
    $<$<COMPILE_LANGUAGE:CXX>:/Library/Developer/CommandLineTools/SDKs/MacOSX.sdk>
    $<$<COMPILE_LANGUAGE:C>:-DNDEBUG>
    $<$<COMPILE_LANGUAGE:CXX>:-DNDEBUG>
    $<$<COMPILE_LANGUAGE:CUDA>:-DNDEBUG>
)
set_target_properties(FuncSolv_core PROPERTIES CXX_EXTENSIONS OFF)
target_compile_features(FuncSolv_core PRIVATE cxx_std_26)
if(CURRENT_COMPILER_FRONTEND_VARIANT STREQUAL "MSVC")
    target_compile_options(FuncSolv_core PRIVATE $<$<CONFIG:Release>:-O2>)
else()
    target_compile_options(FuncSolv_core PRIVATE -O3)
endif()
if(CURRENT_COMPILER_FRONTEND_VARIANT STREQUAL "MSVC")
else()
    target_compile_options(FuncSolv_core PRIVATE -fvisibility=hidden)
endif()
if(CURRENT_COMPILER_FRONTEND_VARIANT STREQUAL "MSVC")
    set_property(TARGET FuncSolv_core PROPERTY
        MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>")
endif()
target_link_options(FuncSolv_core PRIVATE
    -target
    arm64-apple-macos
    -isysroot
    /Library/Developer/CommandLineTools/SDKs/MacOSX.sdk
    -lz
)
# private sourcefiles from sourcebatch c++.build for target FuncSolv_core
target_sources(FuncSolv_core PRIVATE 
    src/MergeSortedLists/MSL.cpp
    src/MergeSortedLists/DivideAndConquerMerge.cpp
    src/MergeSortedLists/HeapKWayMerge.cpp
    src/MergeSortedLists/TwoWayMerge.cpp
    src/WaterVolume/WaterVolume.cpp
    src/WaterVolume/MonotonicStackTrap.cpp
    src/WaterVolume/WaterVolume_ListHight.cpp
    src/WaterVolume/TwoPointerTrap.cpp
    src/IOdata/FastFileReader.cpp
    src/IOdata/AsyncFileWriter.cpp
)

# target
add_library(FuncSolv_tasks OBJECT "")
set_target_properties(FuncSolv_tasks PROPERTIES LIBRARY_OUTPUT_DIRECTORY "${CMAKE_SOURCE_DIR}/build/macosx/arm64/release")
add_dependencies(FuncSolv_tasks FuncSolv_core)
target_include_directories(FuncSolv_tasks PRIVATE
    include
)
target_compile_options(FuncSolv_tasks PRIVATE
    $<$<COMPILE_LANGUAGE:C>:-target>
    $<$<COMPILE_LANGUAGE:CXX>:-target>
    $<$<COMPILE_LANGUAGE:C>:arm64-apple-macos>
    $<$<COMPILE_LANGUAGE:CXX>:arm64-apple-macos>
    $<$<COMPILE_LANGUAGE:C>:-isysroot>
    $<$<COMPILE_LANGUAGE:CXX>:-isysroot>
    $<$<COMPILE_LANGUAGE:C>:/Library/Developer/CommandLineTools/SDKs/MacOSX.sdk>
    $<$<COMPILE_LANGUAGE:CXX>:/Library/Developer/CommandLineTools/SDKs/MacOSX.sdk>
    $<$<COMPILE_LANGUAGE:C>:-DNDEBUG>
    $<$<COMPILE_LANGUAGE:CXX>:-DNDEBUG>
    $<$<COMPILE_LANGUAGE:CUDA>:-DNDEBUG>
)
set_target_properties(FuncSolv_tasks PROPERTIES CXX_EXTENSIONS OFF)
target_compile_features(FuncSolv_tasks PRIVATE cxx_std_26)
if(CURRENT_COMPILER_FRONTEND_VARIANT STREQUAL "MSVC")
    target_compile_options(FuncSolv_tasks PRIVATE $<$<CONFIG:Release>:-O2>)
else()
    target_compile_options(FuncSolv_tasks PRIVATE -O3)
endif()
if(CURRENT_COMPILER_FRONTEND_VARIANT STREQUAL "MSVC")
else()
    target_compile_options(FuncSolv_tasks PRIVATE -fvisibility=hidden)
endif()
if(CURRENT_COMPILER_FRONTEND_VARIANT STREQUAL "MSVC")
    set_property(TARGET FuncSolv_tasks PROPERTY
        MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>")
endif()
target_link_libraries(FuncSolv_tasks PRIVATE
    FuncSolv_core
)
target_link_directories(FuncSolv_tasks PRIVATE
    lib
)
target_link_options(FuncSolv_tasks PRIVATE
    -target
    arm64-apple-macos
    -isysroot
    /Library/Developer/CommandLineTools/SDKs/MacOSX.sdk
    -lz
)
# private sourcefiles from sourcebatch c++.build for target FuncSolv_tasks
target_sources(FuncSolv_tasks PRIVATE 
    build/water_volume_task.cpp
    build/demo_task.cpp
    build/merge_sorted_lists_task.cpp
)

